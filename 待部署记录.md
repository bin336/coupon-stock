# 券库 待部署记录（攒一波一起上 NAS）

> 用途：记录已提交到 GitHub、但**尚未部署到 NAS** 的改动，方便下次批量更新时一次搞定。
> 上次已部署版本：**v3.35**（2026-07-19 上 NAS，运行正常）。

---

## 待部署批次：v3.36（累积中）

| 提交 | 改动 | 类型 |
|------|------|------|
| `80f246e` | 后端启用 gzip 压缩 + 静态/图片缓存（app.js 实测 90,895 B → 25,230 B，约 72%） | 性能 |
| `ed43944` | docker-compose 默认 `network_mode: host`（移除 `ports:` 映射，Tailscale 外网开箱即用） | 部署配置 |

### 本次累积的改动说明
- **gzip 压缩**：所有 >1KB 的响应自动压缩，手机经 Tailscale 慢链路打开明显更快、流量更省。
- **浏览器缓存**：JS/CSS 缓存 10 分钟、二维码截图缓存 7 天；HTML 不缓存，所以更新代码后刷新即生效，不用手动清缓存。
- **host 网络**：以后下载的新包直接 `docker compose up` 就能用 Tailscale 的 `100.x.x.x` 访问，不用再手动改 docker-compose。

---

## 下次部署时照这个清单做

1. 在 GitHub 下载最新 zip（HEAD = `ed43944`）。
2. 解压后**覆盖**到 NAS：`/share/CACHEDEV1_DATA/Container/container-station-data/coupon-stock`，**保留里面的 `./data` 目录不动**。
3. SSH 进 NAS 该目录，执行：
   ```bash
   docker rm -f coupon-stock
   docker compose up -d --build
   ```
4. 手机无痕窗口打开 `http://100.x.x.x:3000` 验证；**重复打开应明显更快**。
5. 部署成功后：清空本文件的「待部署批次」表格，并把《使用文档》顶部「适用版本」改为 **v3.36**、删掉「代码当前领先」那行。

---

## 注意事项
- gzip 只解决**传输体积 + 重复打开**的慢；若慢来自手机流量走 Tailscale 中继（DERP）的底层带宽，代码层面无解，只能连 WiFi 或等信号好。
- 数据全在 `./data`（含二维码截图），重建容器不丢。
- 外网 HTTP 明文不安全，条件允许建议加反向代理 + HTTPS（见《使用文档》第九节）。
