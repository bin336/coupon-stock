# 服务端口（容器内，一般不用改）
PORT=3000

# 数据目录（Docker 中已挂载为卷，无需改）
DATA_DIR=/app/data

# 管理员初始账号（首次启动自动创建；之后请在界面里改密码/新增账号）
ADMIN_USER=admin
ADMIN_PASS=admin123

# JWT 签名密钥：请改成一段随机的长字符串，例如 openssl rand -hex 32
JWT_SECRET=please-change-this-to-a-random-secret-string
